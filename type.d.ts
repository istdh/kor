export type Course = {};

export type TypeRender = {
  children: React.ReactNode;
};

export type TypeCommon = {
  name: string;
  email: string;
  hotline: string;
  logo: {
    url: string;
  };
  address: string;
};
